server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/Neil2V/SPRING-EUREKA-CLOUD-CONFIG-GATEWAY
          search-paths: config-data
  application:
    name: config-service